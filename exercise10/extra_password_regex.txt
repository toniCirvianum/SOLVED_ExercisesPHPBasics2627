Extra password regular expression:

/^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[^A-Za-z0-9]).{8,}$/

Requirements:
- At least one lowercase letter
- At least one uppercase letter
- At least one number
- At least one symbol
- At least 8 characters
